# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'
inhibit_all_warnings!

target 'LLClient' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LLClient
  pod 'Masonry'
  pod 'SnapKit', '~> 5.0.0'
  pod 'Kingfisher', '~> 7.0'
  pod 'LLAccount', :path => './local_pods/LLAccount'
  pod 'LLMain', :path => './local_pods/LLMain'
  pod 'LLCommon', :path => './local_pods/LLCommon'

  target 'LLClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LLClientUITests' do
    # Pods for testing
  end

end
